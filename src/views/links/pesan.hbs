<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/links/pesan/{{link.ID}}" method="POST">
                        <div class="form-group">
                            <p>Nama : {{link.Nama}}</p>
                        <div class="form-group">
                            <input type="text" class="form-control" name="Pesan" placeholder="Isi Pesan" value="{{link.Pesan}}">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Kirim
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>